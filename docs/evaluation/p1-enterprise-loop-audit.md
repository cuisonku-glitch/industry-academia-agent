# P1 企业端核心闭环本地验收记录

> 验收日期：2026-09-02；运行方式：本地 Windows + Conda `industry_agent`；Embedding：CUDA；外部 API：未调用。

## 结论

企业端 P1 第一纵切已形成可运行闭环：企业原话先经过轻量解析和人工确认，再按技术模块检索本地论文，经过证据闸门生成方案、技术路线、转化评估、阶段落地计划和下载文件。系统在缺少量化指标时会保留草案并停在澄清决策门，不把系统建议或未知项伪装成企业事实。

## 已验收的数据契约

- 企业需求保留原文及逐字段证据映射；新增量化目标、测试条件、已有基础、排除路线和未解析片段。
- 技术模块只来自企业显式能力或问题，每个模块保留原文短语和确认状态。
- 每个模块独立形成检索查询，论文证据保留题名、导师、页码、Chunk、相似度和摘录。
- 方案数量由证据覆盖决定；当前证据只能支持一个方案时，不生成虚构的第二、第三方案。
- 路线以节点和边保存，包含输入、动作、输出、验收/退出口径、责任建议、风险和替代路线。
- 转化评估只让已知维度进入分母；工程成熟度和落地约束缺材料时保持未知。
- 来源类型限定为 `enterprise_confirmed`、`paper_evidence`、`system_suggestion` 和 `unknown`。

## 自动化验证

- 运行命令：`python -m unittest discover -s tests -v`
- 结果：76 项通过，0 失败。
- 覆盖：旧版解析、切块、目录、检索、匹配与 RAG 回归；P1 需求解析、证据闸门、路线无环校验、draw.io XML、Agent 顺序、Streamlit 两阶段交互和下载结果。

## 真实本地 GPU 冒烟验收

运行命令：

```powershell
python src/agents/workflow.py --demo --confirm-requirement
```

结果：

- 本地 Embedding 设备：`cuda`。
- 8 个 Agent 全部 `completed`。
- 推荐教师：徐修文；旧版透明匹配分 `58.74/100`；证据审查 `passed`。
- 技术模块：3 个；方案：1 个；方案闸门：`provisional`。
- 技术路线：6 个可编辑节点；`.drawio` XML 可重新解析。
- 证据完整度：60%；已知评估权重：70%。
- 转化决策：`proceed_to_clarification`。原因是指南示例只有“低成本、高灵敏度、大面积制备”等定性表达，没有量化验收指标；这是预期的安全行为。

本地生成文件位于被 `.gitignore` 排除的 `data/processed/agent_runs/`，不会随正常提交公开企业需求、论文摘录或运行结果。

## 尚未完成

- 网页内逐字段修改、版本保存、回退和中断后恢复。
- 指标本体、单位归一和跨测试条件可比性校验。
- 基于真实企业案例和人工金标准的匹配评分校准。
- PDF 报告、论文图片/表格理解、院校端论文工作台和谱系版图。
- 知识产权、法规、安全、财务与工程结论仍需专业人员审查。
